window.BLOODGROUP_CONFIG = window.BLOODGROUP_CONFIG || {
  // Leave these empty for local Apache/PHP usage.
  // For a static Vercel frontend with a separate backend host, set absolute URLs.
  appApiBase: "",
  adminApiBase: "",
  billingApiBase: ""
};
